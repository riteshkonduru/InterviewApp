{
  "name": "interviewapp",
  "version": "0.0.0",
  "description": "An ElasticSearch cluster can contain multiple Indices (databases), which in turn contain multiple Types (tables). These types hold multiple Documents (rows), and each document has Properties (columns).",
  "private": true,
  "scripts": {
    "start": "node ./bin/www"
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "elasticsearch": "^15.3.1",
    "express": "~4.16.0",
    "http-errors": "~1.6.2",
    "http-server": "^0.11.1",
    "jade": "~1.11.0",
    "morgan": "~1.9.0",
    "pug": "^2.0.3"
  }
}
